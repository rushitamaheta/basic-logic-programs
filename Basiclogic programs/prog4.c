/*4. Find Area of Square formula : a = a2*/
#include<stdio.h>
int main(){
	float length,area;
	printf("enter length");
	scanf("%f",&length);
	
	area =  (length*length);
		printf("%f",area);
	
}
